#include<stdio.h>

int main()
{   
   /* int x=3;
    int y=3;
    int z=0;
    z+= pow(x++,y--);
    z+= pow(++x,y--);
    z+= pow(x++,--y);

    printf("z: %d", z);

    char alpha='G';
    int result;   
    result = alpha-'A';
    result /=13;
    result = -result*13 +(1- result)*13;
    printf("result#1: %c \n", result+alpha);

    double area;
    double volume;
    double length=2;
    float pentagon=6.88;
    volume=1/4*pentagon*pow(length,3);
    area=5*pow(length,2)+1/2*pow(length,2)*pentagon;
    printf("volume :%.1lf\n",volume);
    printf("area :%.1lf\n", area);

    int a=5,b=-7,c=0,d;
    d=++a&&++b || ++c; //  because || ke pehle one aa gya toh ye age ki reading lega hi nhi;
    printf("\n %d%d%d%d",a,b,c,d);

    int x=5,y=4,z=3,f1,f2,f3;
    f1 = x/y + x/z;
    f1 = 1/f1 *x;

    f2= y/x+y/z;
    f2= f2%2*y;
    f3= 1-(f2+f1+2)%(f2+f1+1);
    f3*=z;

    printf("%d", f1+f2+f3);*/

    
    printf("Surya\v \vbal");
    return 0;
}