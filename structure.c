#include<stdio.h>
#include<string.h>
int main(){
   /* struct book{
        int noOfPages;
        float price;
        char name[20];
        char authorName[20];
    };   // you can declare the objects before semicolon.. like   }a,b,c;

    struct book a;
    a.noOfPages=100;
    a.price=411.56;
    strcpy(a.name,"Atomic Habits");        // otherwise,you can fill all elements in the array one by one but you can't directly change or put some value in the array , ones you declare it...
    strcpy(a.authorName,"James Clear");

    printf("%d\n",a.noOfPages);
    printf("%f\n",a.price);
    printf("%s\n",a.name);
    printf("%s\n",a.authorName);*/

    
    return 0;
}